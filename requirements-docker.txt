# django et drf, le duo de choc pour notre api
Django==5.0.1
djangorestframework==3.14.0

# connexion postgresql, le pont entre django et notre bdd
psycopg2-binary==2.9.9

# pour gerer les variables d'environnement proprement
python-dotenv==1.0.0

# cors pour que le frontend puisse causer avec notre api
django-cors-headers==4.3.1

# filtrage des requetes api, bien pratique
django-filter==23.5

# pour les uuid en tant que cle primaire
uuid==1.30

# on garde pandas et numpy pour l'etl plus tard
pandas==2.1.4
numpy==1.26.2

# pour le scraping france travail
requests==2.31.0
beautifulsoup4==4.12.2

# scikit-learn pour le ml quand on y sera
scikit-learn==1.3.2
